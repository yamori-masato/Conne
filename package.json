{
  "name": "project",
  "version": "1.0.0",
  "description": "You can post articles using this application.",
  "main": "index.js",
  "scripts": {
    "build": "cd front && npm install",
    "heroku-postbuild": "npm run build && echo 'Client built!'"
  }
}